"use client";
import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    mode: "light",
    primary: {
      main: "#8bfcb0",
    },
    secondary: {
      main: "#ff8bd3",
    },
  },
});

export default theme;
